{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "pnpm install && pnpm exec playwright install --with-deps chromium",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist"
}
